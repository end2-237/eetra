import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/api/', // Optionnel : cachez vos routes API
    },
    sitemap: 'https://eetra.buyticle.com/sitemap.xml',
  }
}